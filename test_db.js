import { createClient } from '@supabase/supabase-js'; const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function check() { const { data: s } = await supabase.from('safety_suggestions').select('*').limit(1); const { data: n } = await supabase.from('near_misses').select('*').limit(1); console.log('S:', s); console.log('N:', n); } check();
