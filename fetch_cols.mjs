import fs from 'fs'; const env = Object.fromEntries(fs.readFileSync('.env', 'utf-8').split('\n').filter(Boolean).map(l => l.split('='))); fetch(env.VITE_SUPABASE_URL + '/rest/v1/safety_reports?id=eq.1', { method: 'PATCH', headers: { apikey: env.VITE_SUPABASE_ANON_KEY.trim(), 'Content-Type': 'application/json' }, body: JSON.stringify({ feedback: 'test', status: '답변완료' }) }).then(async r => { console.log('Status:', r.status); if (r.status !== 204) console.log(await r.json()); });
