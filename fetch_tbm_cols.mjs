import fs from 'fs'; const env = Object.fromEntries(fs.readFileSync('.env', 'utf-8').split('\n').filter(Boolean).map(l => l.split('='))); fetch(env.VITE_SUPABASE_URL + '/rest/v1/tbm_submissions', { method: 'POST', headers: { apikey: env.VITE_SUPABASE_ANON_KEY.trim(), 'Content-Type': 'application/json' }, body: JSON.stringify({ id: 1, user_id: 'a', user_name: 'a', company_id: 'a', date: 'a', type: 'a', title: 'a', data: {}, files: [], participant_signatures: [] }) }).then(async r => console.log(await r.json(), r.status));
